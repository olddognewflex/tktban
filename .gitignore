__pycache__/
*.pyc
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
# fixture board state sidecars are regenerated by tkt verbs during manual testing
tests/fixtures/.sdlc/state/
# repo-root .sdlc is generated per-machine by odnf/.sdlc/_gen_configs.py (absolute
# paths) — leading slash so it does NOT match the committed tests/fixtures/.sdlc
/.sdlc/
